const initialState = {

}

export const loginReducer = (state: InitialStateType = initialState, action: LoginActionsTypes) => {
    switch (action.type) {
        default:
            return state
    }
}

// actions



// thunks



// types
export type InitialStateType = typeof initialState
type LoginActionsTypes=any